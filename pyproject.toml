[project]
name = "claudex-learn"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "curl-cffi>=0.16.3",
    "pyyaml>=6.0.3",
    "trafilatura>=2.2.0",
    "youtube-transcript-api>=1.2.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
